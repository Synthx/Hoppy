include: package:pedantic/analysis_options.yaml

analyzer:
  exclude:
    - '**/*.g.dart'
    - '**/*.freezed.dart'
    - 'lib/generated/**/*.dart'
  errors:
    # enable when all package successfully migrate to null safety
    # one missing : charts_flutter
    import_of_legacy_library_into_null_safe: ignore
